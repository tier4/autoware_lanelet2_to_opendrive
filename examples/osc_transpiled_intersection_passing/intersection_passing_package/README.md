# intersection-passing-package

Scenario package transpiled from `/home/user/autoware_lanelet2_to_opendrive/examples/osc_transpiled_intersection_passing/intersection_passing.osc` by `autoware_carla_scenario.openscenario_dsl_frontend`.

## Install & run

```bash
uv pip install -e .
uv run scenario scenario=intersection_passing/default map=nishishinjuku
```
