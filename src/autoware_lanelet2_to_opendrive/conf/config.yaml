# Hydra configuration for Lanelet2 to OpenDRIVE conversion
# Usage: uv run python -m autoware_lanelet2_to_opendrive.main map=<map_name> target=<target_name>

defaults:
  - _self_
  - map: example
  - target: default

# Input and output paths (can be overridden via CLI)
input_map_path: ???  # Required: path to input Lanelet2 OSM file
output_map_path: null  # Optional: output file path

# Global settings
dry_run: false    # If true, only validate without saving output
verbose: false    # Enable detailed logging

# Geometry settings
param_poly3_num_segments: 30  # Number of ParamPoly3 segments per road (default: 30, higher = smoother but larger file)

# Adaptive subdivision settings - automatically detect and handle large heading changes
enable_adaptive_subdivision: false  # Enable adaptive ParamPoly3 segment subdivision
max_heading_change_deg: 30.0  # Maximum allowed heading change per segment (degrees)
max_subdivision_iterations: 3  # Maximum number of subdivision iterations
